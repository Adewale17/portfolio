Replace `profile.jpg` in this folder with Degbaun's portrait. The site shows a styled fallback until the image is added.

Add project screenshots to the `projects` folder and reference their paths in `src/data/portfolio.js`.
